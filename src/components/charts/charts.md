```js
const config = {};
const data = {};
const domain = {};
const dataOptions = {};
const dataSelected = {};
const loading = false;
<Chart
  type="line"
  config={config}
  data={data}
  domain={domain}
  dataOptions={filters}
  dataSelected={filtersSelected}
  height={500}
  loading={loading}
/>
```
