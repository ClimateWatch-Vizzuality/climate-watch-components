### Check-input bit example:

```js
const styles = require('./check-input-theme.scss');
<CheckInput
    className={styles.checkbox}
    label={'Subscribe'}
    toggleFirst
    disabled={false}
/>
```
