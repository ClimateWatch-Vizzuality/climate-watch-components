### Check-input bit example:

```js
const styles = require('./check-input-styles.scss');
<CheckInput
    className={styles.checkbox}
    label={'Subscribe'}
    toggleFirst
    theme={styles}
    disabled={false}
/>
```